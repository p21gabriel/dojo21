{% extends "layout/layout" %}

{% block title %}Dojo21 | Meus objetivos{% endblock title %}

{% block content %}
    <section id="objectives" class="mt-50 main-section-page">
        <h2 class="page-title">Meus objetivos</h2>

        <table class="text-center table-control">
            <div class="text-right adicionar">
                <a class="blue" href="/dashboard">Dashboard</a>
                <a class="green" href="/objective/add">Adicionar</a>
            </div>

            <thead>
                <tr>
                    <th>Titulo</th>
                    <th>Descrição</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach($objectives as $objective): ?>
                    <tr style="border: 1px solid #ccc">
                        <td class="text-left">{{ $objective->getTitle() }}</td>
                        <td class="text-left">{{ $objective->getDescription() }}</td>
                        <td class="text-left">
                            <a class="btn btn-table blue" href="/objective/{{ $objective->getId() }}/key_results">Resultados chaves</a>
                            <a class="btn btn-table yellow" href="/objective/{{ $objective->getId() }}/edit">Editar</a>
                            <?php if(!$objective->getKeyResults()): ?>
                                <a class="btn btn-table red delete" href="#" data-id="{{ $objective->getId() }}">Remover</a>
                            <?php endif ?>
                        </td>
                    </tr>
                <?php endforeach ?>
            </tbody>
        </table>
    </section>
{% endblock content %}

{% block scripts %}
    <script src="{{ assets('js/objective.js') }}?v={{ version() }}" type="text/javascript"></script>
{% endblock scripts %}
